# NHibernate Simple Example
The aim of this repository is to have a basic example of code using NHibernate.
In this example we have a `Student' table and we use NHibernate to add recods and to read records from the database table.
